interface AnnouncmentTheme {
  base: string
}

const announcmentTheme: AnnouncmentTheme = {
  base: 'inline-flex items-center rounded-lg px-3 py-1 text-sm font-medium gap-2 text-black',
}

export { announcmentTheme, type AnnouncmentTheme }
