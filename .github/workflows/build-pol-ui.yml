name: Build pol-ui

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        required: false
        default: '22'
      pnpm-version:
        type: string
        required: false
        default: '9'

jobs:
  build:
   
